name: Check plugin sync

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: Check mirrored plugin content
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun run check
